# {{ project_name }}

Generated by `azure-functions-scaffold`.

Template: `durable`

Preset: `{{ preset_name }}`

Python: `{{ python_version }}`

## Development

```bash
python -m venv .venv
. .venv/bin/activate
pip install -e .{% if include_ruff or include_mypy or include_pytest %}[dev]{% endif %}
{% if include_ruff -%}
ruff check .
ruff format --check .
{% endif -%}
{% if include_mypy -%}
mypy .
{% endif -%}
{% if include_pytest -%}
pytest
{% endif -%}
func start
```

## Local Durable Functions development

Durable Functions requires `AzureWebJobsStorage`. Use Azurite for local
development (`UseDevelopmentStorage=true` in `local.settings.json`).
